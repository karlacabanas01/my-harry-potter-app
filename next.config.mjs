/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'ik.imagekit.io',
      'www.wizardingworld.com',
      'images.unsplash.com',
      'raw.githubusercontent.com',
    ],
  },
};

export default nextConfig;
